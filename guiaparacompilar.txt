**** Usuário MySql = 'admin', senha = '$enhaF0rt3'

python -m venv venv

venv\Scripts\activate

pip install -r requirements.txt
python setup_database.py (para reiniciar o banco antigo)

python main.py (para rodar o banco atual)